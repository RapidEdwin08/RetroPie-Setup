diff -ur a/src/am_map.cpp b/src/am_map.cpp
--- a/src/am_map.cpp	UZDoom Source trunk 5104cc43
+++ b/src/am_map.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -171,7 +171,7 @@
 CVAR(Bool, am_showtime, true, CVAR_ARCHIVE);
 CVAR(Bool, am_showtotaltime, false, CVAR_ARCHIVE);
 CVAR(Bool, am_showlevelname, true, CVAR_ARCHIVE);
-CVAR(Int, am_colorset, 0, CVAR_ARCHIVE);
+CVAR(Int, am_colorset, 1, CVAR_ARCHIVE);
 CVAR(Bool, am_customcolors, true, CVAR_ARCHIVE);
 CVAR(Int, am_map_secrets, 1, CVAR_ARCHIVE);
 CVAR(Int, am_drawmapback, 1, CVAR_ARCHIVE);
diff -ur a/src/common/audio/music/i_music.cpp b/src/common/audio/music/i_music.cpp
--- a/src/common/audio/music/i_music.cpp	UZDoom Source trunk 5104cc43
+++ b/src/common/audio/music/i_music.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -70,7 +70,7 @@
 // Maximum volume of MOD/stream music.
 //==========================================================================
 
-CUSTOM_CVARD(Float, snd_musicvolume, 0.5, CVAR_ARCHIVE|CVAR_GLOBALCONFIG, "controls music volume")
+CUSTOM_CVARD(Float, snd_musicvolume, 0.7, CVAR_ARCHIVE|CVAR_GLOBALCONFIG, "controls music volume")
 {
 	if (self < 0.f)
 		self = 0.f;
diff -ur a/src/common/console/c_console.cpp b/src/common/console/c_console.cpp
--- a/src/common/console/c_console.cpp	UZDoom Source trunk 5104cc43
+++ b/src/common/console/c_console.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -134,7 +134,7 @@
 static char *work = NULL;
 static int worklen = 0;
 
-CUSTOM_CVAR(Int, con_scale, 0, CVAR_ARCHIVE)
+CUSTOM_CVAR(Int, con_scale, 4, CVAR_ARCHIVE)
 {
 	if (self < 0) self = 0;
 }
diff -ur a/src/common/rendering/v_video.cpp b/src/common/rendering/v_video.cpp
--- a/src/common/rendering/v_video.cpp	UZDoom Source trunk 5104cc43
+++ b/src/common/rendering/v_video.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -142,7 +142,7 @@
 }
 
 
-CUSTOM_CVAR(Int, uiscale, 0, CVAR_ARCHIVE | CVAR_NOINITCALL)
+CUSTOM_CVAR(Int, uiscale, 2, CVAR_ARCHIVE | CVAR_NOINITCALL)
 {
 	if (self < 0)
 	{
diff -ur a/src/common/statusbar/base_sbar.cpp b/src/common/statusbar/base_sbar.cpp
--- a/src/common/statusbar/base_sbar.cpp	UZDoom Source trunk 5104cc43
+++ b/src/common/statusbar/base_sbar.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -57,9 +57,9 @@
 IMPLEMENT_CLASS(DHUDFont, false, false);
 
 
-CVAR(Color, crosshaircolor, 0xff0000, CVAR_ARCHIVE);
-CVAR(Int, crosshairhealth, 2, CVAR_ARCHIVE);
-CVARD(Float, crosshairscale, 0.5, CVAR_ARCHIVE, "changes the size of the crosshair");
+CVAR(Color, crosshaircolor, 0x00ff1e, CVAR_ARCHIVE);
+CVAR(Int, crosshairhealth, 1, CVAR_ARCHIVE);
+CVARD(Float, crosshairscale, 0.25, CVAR_ARCHIVE, "changes the size of the crosshair");
 CVAR(Bool, crosshairgrow, false, CVAR_ARCHIVE);
 
 CUSTOM_CVARD(Float, hud_scalefactor, 1.f, CVAR_ARCHIVE, "changes the hud scale")
diff -ur a/src/console/c_notifybuffer.cpp b/src/console/c_notifybuffer.cpp
--- a/src/console/c_notifybuffer.cpp	UZDoom Source trunk 5104cc43
+++ b/src/console/c_notifybuffer.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -59,7 +59,7 @@
 CVAR(Bool, con_centernotify, false, CVAR_ARCHIVE)
 CVAR(Bool, con_pulsetext, false, CVAR_ARCHIVE)
 
-CUSTOM_CVAR(Int, con_scaletext, 0, CVAR_ARCHIVE)		// Scale notify text at high resolutions?
+CUSTOM_CVAR(Int, con_scaletext, 4, CVAR_ARCHIVE)		// Scale notify text at high resolutions?
 {
 	if (self < 0) self = 0;
 }
diff -ur a/src/g_game.cpp b/src/g_game.cpp
--- a/src/g_game.cpp	UZDoom Source trunk 5104cc43
+++ b/src/g_game.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -206,7 +206,7 @@
 
 #define SLOWTURNTICS	6 
 
-CVAR (Bool,		cl_run,			false,	CVAR_GLOBALCONFIG|CVAR_ARCHIVE)		// Always run?
+CVAR (Bool,		cl_run,			true,	CVAR_GLOBALCONFIG|CVAR_ARCHIVE)		// Always running from evil?
 CVAR (Bool,		freelook,		true,	CVAR_GLOBALCONFIG|CVAR_ARCHIVE)		// Always mlook?
 CVAR (Bool,		lookstrafe,		false,	CVAR_GLOBALCONFIG|CVAR_ARCHIVE)		// Always strafe with mouse?
 CVAR (Float,	m_forward,		1.f,	CVAR_GLOBALCONFIG|CVAR_ARCHIVE)
@@ -219,7 +219,7 @@
 CVAR (Float, cl_analog_sensitivity_yaw,		1.f,	CVAR_GLOBALCONFIG|CVAR_ARCHIVE)
 CVAR (Float, cl_analog_sensitivity_pitch,	0.6f,	CVAR_GLOBALCONFIG|CVAR_ARCHIVE)
 
-CVAR (Bool, cl_analog_run, true, CVAR_GLOBALCONFIG|CVAR_ARCHIVE)
+CVAR (Bool, cl_analog_run, false, CVAR_GLOBALCONFIG|CVAR_ARCHIVE)		// if true, does not respect toggle cl_run
 CVAR (Bool, cl_analog_straferun, false, CVAR_GLOBALCONFIG|CVAR_ARCHIVE)
 
 int 			turnheld;								// for accelerative turning 
@@ -2260,7 +2260,7 @@
 
 CVAR (Int, autosavenum, 0, CVAR_NOSET|CVAR_ARCHIVE|CVAR_GLOBALCONFIG)
 static int nextautosave = -1;
-CVAR (Int, disableautosave, 0, CVAR_ARCHIVE|CVAR_GLOBALCONFIG)
+CVAR (Int, disableautosave, 2, CVAR_ARCHIVE|CVAR_GLOBALCONFIG)
 CVAR (Bool, saveloadconfirmation, true, CVAR_ARCHIVE | CVAR_GLOBALCONFIG) // [mxd]
 CUSTOM_CVAR (Int, autosavecount, 4, CVAR_ARCHIVE|CVAR_GLOBALCONFIG)
 {
diff -ur a/src/g_statusbar/shared_sbar.cpp b/src/g_statusbar/shared_sbar.cpp
--- a/src/g_statusbar/shared_sbar.cpp	UZDoom Source trunk 5104cc43
+++ b/src/g_statusbar/shared_sbar.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -105,7 +105,7 @@
 
 
 // Stretch status bar to full screen width?
-CUSTOM_CVAR (Int, st_scale, 0, CVAR_ARCHIVE)
+CUSTOM_CVAR (Int, st_scale, 2, CVAR_ARCHIVE)
 {
 	if (self < -1)
 	{
@@ -123,8 +123,8 @@
 EXTERN_CVAR(Bool, hud_aspectscale)
 
 CVAR (Bool, crosshairon, true, CVAR_ARCHIVE);
-CVAR (Int, crosshair, 0, CVAR_ARCHIVE)
-CVAR (Bool, crosshairforce, false, CVAR_ARCHIVE)
+CVAR (Int, crosshair, 2, CVAR_ARCHIVE)
+CVAR (Bool, crosshairforce, true, CVAR_ARCHIVE)
 CUSTOM_CVAR(Int, am_showmaplabel, 2, CVAR_ARCHIVE)
 {
 	if (self < 0 || self > 2) self = 2;
diff -ur a/src/menu/doommenu.cpp b/src/menu/doommenu.cpp
--- a/src/menu/doommenu.cpp	UZDoom Source trunk 5104cc43
+++ b/src/menu/doommenu.cpp	2025-10-28 17:33:18.150096509 -0400
@@ -71,7 +71,7 @@
 EXTERN_CVAR(Bool, haptics_do_menus)
 EXTERN_CVAR(Float, hud_scalefactor)
 
-CVAR(Bool, m_simpleoptions, true, CVAR_ARCHIVE|CVAR_GLOBALCONFIG);
+CVAR(Bool, m_simpleoptions, false, CVAR_ARCHIVE|CVAR_GLOBALCONFIG);
 CVAR(Bool, m_simpleoptions_view, true, 0);
 
 typedef void(*hfunc)();
